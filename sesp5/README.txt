Die main-v1.cpp ist meine version gewesen die funtionierte bei mir so 
mit der makefile, jedoch fehlt da noch der aufgabenteil mit der zeitmessung
und dem scheduling.

Die main-v2.cpp ist von meinem praktikumspartner, das ist die komplette
version aber er hat mir gesagt man muss noch irgend was verändern in den
vorgegebenen dateien damit es läuft. ich glaube das war in der cmessage.h
bei der setparameter4 oder getparameter4 funktion irgendwas mit 32byte pointer.